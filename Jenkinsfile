pipeline {
  agent any
    stages {
        stage("checkout the code ") {
            steps {
                checkout scm
            }
     
        }


        stage("checkout the code ") {
            steps {
                sh "mvn clean install"
            }

        }



    }
}
